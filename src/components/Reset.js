import React from "react";

class Reset extends React.Component {
    render() {
        return (
            <button type="button" className="btn btn-primary btn-reset" >reset</button>
        );
    }
}

export default Reset;